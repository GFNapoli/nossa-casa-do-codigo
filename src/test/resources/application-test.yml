#datasources:
#  default:
#    url: jdbc:tc:postgresql:12:///postgres
#    driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver

datasources:
  default:
    url: jdbc:h2:mem:testob;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driverClassName: org.h2.Driver

#mostra log transacional
logger:
  levels:
    io.micronaut.transaction: DEBUG